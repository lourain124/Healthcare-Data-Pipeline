# Cuadernos

Los CSV listos para experimentar estan en `../data/final/` (por ejemplo `thyroid_model_ready.csv`). Puedes abrir Jupyter o VS Code en esta carpeta y cargar ese archivo con pandas para entrenar modelos o explorar distribuciones.
